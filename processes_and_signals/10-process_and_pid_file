#!/usr/bin/env bash
# Task 9


trap "echo 'Y U no love me?!'" SIGINT

trap "echo 'I heat the kill command'; rm -f /var/run/myscript.pid" SIGTERM

trap "rm -f /var/run/myscript.pid" SIGQUIT

echo $$ > /var/run/myscript.pid

while true; 
do
	echo "To infinity and beyond"
done
